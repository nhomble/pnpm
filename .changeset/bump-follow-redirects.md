---
"pnpm": patch
---

Bump `follow-redirects` override to `>=1.16.0` to address a moderate-severity GHSA advisory flagged by the audit workflow (authorization header leak on cross-domain redirects).
